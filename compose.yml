version: "3.9"

services:
  edge-proxy:
    build:
      context: .
      dockerfile: dockerfile
    container_name: edge-proxy
    networks:
      - rede_zelchi
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - certs:/app/certs
      - ./config.yml:/app/config.yml:ro

volumes:
  certs:

networks:
  rede_zelchi:
    external: true
